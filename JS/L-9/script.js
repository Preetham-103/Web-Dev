console.log("Hello World");

const child = document.body.childNodes;
console.log(child);

/* document.body.childNodes -> gives all the childNodes
document.body.childNodes[0].childNodes -> gives the childNodes of the first childNode
document.body.firstElemenetChild -> give the first child element(div and etc).
document.body.lastElementChild -> gives the ladt child element.
*/
